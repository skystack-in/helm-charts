apiVersion: v1
kind: Service
metadata:
  name: {{ include "database-backup-s3.fullname" . }}
  labels:
    {{- include "database-backup-s3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "database-backup-s3.selectorLabels" . | nindent 4 }}
